[pytest]
testpaths = tests
addopts = -ra --alluredir=allure-results --reruns 1 --reruns-delay 5
markers =
    smoke: quick smoke-test subset
    regression: full regression suite
filterwarnings =
    ignore::DeprecationWarning
